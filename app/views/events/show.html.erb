<%= link_to "Show All events", events_path %>

<h1><%= @event.name %></h1>
<h2><%= @event.date %></h2>

<%= link_to 'Edit', edit_event_path %> | <%= link_to 'Delete', event_path(@event), method: :delete, data: {confirm: "Are you sure you want to delete the location '#{@event.name}'? This cannot be undone!"} %>
